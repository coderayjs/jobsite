# backend/.env
# Server Configuration
PORT=8000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb+srv://jobsite:Sigmaskibididopedev@cluster0.jpw4u.mongodb.net/
# For MongoDB Atlas:
# MONGODB_URI=mongodb+srv://your_username:your_password@cluster.mongodb.net/job-board

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE=30d

# Email Configuration (using Gmail as example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_specific_password
SMTP_SECURE=false

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:3000

# Cloud Storage (if using cloud storage for files)
CLOUD_NAME=your_cloudinary_name
CLOUD_API_KEY=your_cloudinary_api_key
CLOUD_API_SECRET=your_cloudinary_api_secret
